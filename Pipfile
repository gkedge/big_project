[[source]]
name = "pypi"
url = "https://pypi.iod.core/simple"
verify_ssl = true

[dev-packages]
pytest = "==5.4.1"

[packages]
# The pytest (or one of its dependencies) is bringing in a version of `atomicwrites` for which there does seem
# to be available on our mirror.  I am forcing a version that I know exists.
atomicwrites = ">=1.3.0"

[requires]
python_version = "3.7"
